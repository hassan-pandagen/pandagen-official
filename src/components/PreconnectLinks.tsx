"use client";

export default function PreconnectLinks() {
  // Removed all preconnects - they were unused and causing PageSpeed warnings
  // Third-party scripts will connect when needed
  return null;
}
